                  
                  {sourceFormat === 'vap' && exportTargetFormat === 'vap' && audioFile && (
                      <button
                        onClick={handleFastAudioReplace}
                        disabled={isExporting}
                        className="w-full mt-3 py-2 px-3 bg-gradient-to-r from-emerald-500 to-teal-600 hover:from-emerald-600 hover:to-teal-700 text-white rounded-xl text-[11px] font-bold transition-all shadow-lg flex items-center justify-center gap-2 disabled:opacity-50"
                      >
                        <Zap className="w-4 h-4" />
                        <span>دمج سريع وتصدير VAP (بدون إعادة معالجة)</span>
                      </button>
                  )}
